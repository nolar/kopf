name: Experiment
on:
  push:
  pull_request:

jobs:
  experiment:
    name: Experimental secrets
    runs-on: ubuntu-20.04
    env:
      XYZ: ${{ secrets.experimental_secret }}
    steps:
      - run: env | sort | grep XYZ
      - run: env | sort | grep XYZ | sed -e 's/ /_/g'
